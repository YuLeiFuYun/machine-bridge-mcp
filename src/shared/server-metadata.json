{
  "name": "machine-bridge-mcp",
  "protocolVersion": "2026-07-28",
  "supportedProtocolVersions": [
    "2026-07-28"
  ],
  "toolSchemaGeneration": 3,
  "instructions": [
    "You are connected to a local workspace through machine-bridge-mcp.",
    "Use resolve_task_capabilities only when the task needs local skill/registered-command discovery, application/browser routing, or refreshed project instructions. Straightforward file, Git, and shell work should use the already exposed tools directly instead of adding a resolver call. When capability routing is useful, pass the current user request and target path and reuse refresh.fingerprint as known_refresh_fingerprint to omit unchanged static instructions. Its execution_routing is bounded, set-level, and advisory: direct Bash remains the general escape hatch whenever the effective policy allows it. MCP transport connections are not conversation identity; refresh task-specific ranking when routing is actually needed rather than assuming prior requests established context.",
    "Before any GitHub read or mutation, apply the effective project control-plane instructions. When a project requires local git, gh, and gh api through Machine Bridge, never use a hosted GitHub connector or ChatGPT GitHub plugin; if the local control plane is unavailable, stop rather than substitute another GitHub tool.",
    "MCP uses 2026-07-28 request-scoped protocol natively. Remote HTTP also accepts stateless initialization-era 2025-06-18 and 2025-11-25 compatibility for hosts that have not migrated yet; that compatibility never creates MCP session IDs or replay state. stdio remains current-only. Clients using 2026-07-28 use server/discover and per-request metadata; session_bootstrap remains an explicit refresh tool for project facts, local instructions, and capability routing.",
    "For existing-browser work, use the paired Machine Bridge browser extension. It operates the user current Chromium profile and tabs, can inspect DOM/source, fill complex forms, and inject sensitive text from registered local resources without returning those values.",
    "For local application work, use structured application discovery and Accessibility actions. Arbitrary caller-supplied AppleScript or JavaScript is intentionally not accepted.",
    "Remote mode uses a Cloudflare relay; stdio mode runs on the local machine. File and command operations execute on the user's local runtime, not in the Worker.",
    "In remote multi-account mode, account authority is the intersection of the authenticated account role and the daemon capability ceiling. Only authorization.effective_policy and authorization.effective_tools describe that account authority; daemon.policy is not the account permission.",
    "Use server_info with detail=summary for routine version, authority-profile, readiness, relay, pending-capacity, and timeout checks. Request the default/full projection only when exact effective tools, OAuth metadata, account identity, or full observability counters are needed. For a recovered relay interruption, compare close-to-ready outage duration with previous_ready_inbound_silence_ms: the latter captures the bounded pre-close interval in which a previously ready transport stopped proving inbound activity.",
    "Filename sensitivity is not classified by this server; the MCP host, connector gateway, local OS, or endpoint-security software may enforce additional independent rules.",
    "A named full profile is canonical: it always enables writes, shell execution, unrestricted paths, the full parent environment, absolute paths, and the complete tool catalog. Any explicit narrowing is stored as custom.",
    "The local daemon may advertise the full catalog as its capability ceiling. In remote mode, the Worker relays only the intersection allowed by the authenticated account role; a connector host may then expose an even smaller subset that Machine Bridge cannot observe or override. Conversation/surface app routing and host-cached action/tool snapshots are host-owned state, not daemon authority.",
    "Use diagnose_runtime to distinguish a request that reached the daemon from local filesystem, process-spawn, shell, managed-job-storage, and resource failures. A tool call blocked before any response cannot be diagnosed by the server; do not call that a platform disable without host-side evidence. Conversely, an execution failure that includes a child exit code or bounded child stdout/stderr proves the local process was spawned; for SSH or another nested command, treat an allowlist/forced-command refusal in that output as downstream target evidence rather than Machine Bridge policy denial. If a fresh supported conversation can invoke the same app/tool while an older conversation cannot, investigate the host conversation/surface route or cached action/tool snapshot before changing Machine Bridge policy.",
    "Never request or return secret-file contents when a local resource alias can be used. Resources are registered through the local machine-mcp CLI; under canonical full policy, generate_ssh_key_resource can generate and register an Ed25519 key without returning private content and may be injected by path, stdin, or environment without entering MCP arguments.",
    "stage_job persists a validated non-running draft only. It is not an approval workflow and cannot be promoted from the terminal; execution requires owner-authorized start_job or an explicit local machine-mcp job submit PLAN.json operation.",
    "Hosted read-only status and diagnostic tools must not be used as busy loops. For a known managed job, use read_job's server-side paced long-poll by default: a relay-origin active read waits inside Machine Bridge for up to the advertised managed-job read interval and returns earlier when meaningful status/phase progress or terminal state is observed; use wait_ms=0 only for an intentional immediate checkpoint. Interactive process sessions use the separately paced read_process contract: the one-second actual output/exit blocking cap remains, while a repeated would-block request inside the fifteen-second cooldown stays inside that same MCP call until output/exit or the cooldown boundary rather than returning a rapid running checkpoint. Bounded same-response follow-up is allowed when the current task needs terminal state or additional output. Do not switch among list_jobs, server_info, diagnose_runtime, or other status surfaces merely to evade pacing. Do not infer or preempt a host/tool deadline from elapsed wall-clock time. While tool calls continue to be accepted and the task still needs the result, bounded same-response follow-up may continue; hand progress back only after an actual host/tool boundary is observed, external input or authorization is required, or the user explicitly requested a checkpoint.",
    "For ordinary one-step remote process work whose child execution fits the 600-second process limit, exec_command, run_process, and run_local_command already commit a durable managed job and return a job_id. Acceptance transfers execution to durable ownership without forcing the current assistant response to end. When the current task needs the result, use bounded same-response read_job follow-up until terminal state; do not busy-loop or use repeated list_jobs as a substitute polling surface, and do not infer a host/tool deadline from elapsed wall-clock time. Cooperative machine-user admission is a separate pre-spawn interval and may wait up to 30 minutes; read_job.current_phase=resource_admission means no child for that step has started yet. Use start_job for owner-authorized multi-step plans, job-scoped temporary_files, explicit resource injection, idempotent finally_steps, or a step that legitimately needs an execution budget above 600 seconds. Detached jobs continue after MCP disconnects and daemon replacement; return job_id/status/current_phase for later recovery only after an actual host/tool boundary is observed, external input or authorization is required, or the user explicitly requested a checkpoint.",
    "Prefer sending remote shell programs through a process stdin rather than creating remote helper files. If temporary files are necessary, use {{temp:name}} or explicit finally_steps.",
    "Managed-job resource redaction is defense in depth, not a guarantee against transformed or partial secret output. Use capture_output=discard for steps that may echo credentials.",
    "Do not use shell encoding, renaming, or alternate tools to bypass host or platform safety policy.",
    "run_process avoids shell parsing but is not an OS sandbox. exec_command is exposed only in shell mode and has the local user's authority.",
    "Do not impose a one-checkpoint turn boundary and do not infer a host/tool deadline from elapsed wall-clock time; continue bounded hosted follow-up while calls are accepted and the task still needs the result. WebSocket remains the preferred daemon transport: a protocol-level Ping is attempted every 5 seconds, its full 10-second Pong deadline begins only after the local WebSocket sender confirms that Ping was written, and a separate 30-second dispatch bound detects a locally stuck send queue. One missed dispatched Pong enters a 15-second application-heartbeat confirmation stage before WSS is rebuilt. Only protocol Pong or application Pong is bidirectional transport proof; unrelated inbound application traffic updates receive-side liveness but cannot clear transport suspicion. Diagnosed local event-loop stalls use their separate recovery grace. If no verified WSS is ready, the same root-certified ephemeral device identity can establish the signed HTTPS daemon fallback. Fallback requests bind the fixed route/origin/server/version, one-time 30-second nonce and timestamp, and exact body SHA-256; they use a 7-second request deadline, 12-second liveness window, 1-second ordinary ready cadence, a 5-second standby-prewarm cadence, bounded 1/2/4/5-second failure backoff, and a hard 750 ms minimum request-start interval. Candidate, probing, and verified-ready are distinct. A signed same-instance takeover may retire a Worker-side zombie WSS only after candidate preconditions pass. The daemon reconciles resume_calls, processes ready_ack, returns sequenced https_ready, and only then returns resume_calls_ack; the Worker does not dispatch ordinary work through HTTPS before verified readiness. Bidirectional contiguous transport sequences make an HTTP response retry retransmit the same envelope and discard duplicates before business handling. A resume_calls_ack.missing_ids entry proves the same daemon has neither active-call nor retained-result ownership for that ID after replacement readiness; while the initiating request remains live and at least one second of the original execution budget remains, the Worker may retransmit exactly that same call ID, arguments, authority, and a reduced timeout. If safe redelivery cannot be accepted it falls back to retryable unavailable with side_effects_started=false. Calls that may have executed, retained results, different-daemon calls, and ambiguous mutations are never automatically replayed. A new daemon-backed call may wait at most 15 seconds for verified recovery, and that measured recovery interval is deducted from the original execution budget rather than extending the hosted foreground envelope. The separate application heartbeat remains 25 seconds with a 75-second application-silence timeout, and Worker WebSocket daemon liveness is a wider 90-second fallback. Ordinary daemon-backed tools default to 20 seconds of remote execution plus a separate five-second Worker settlement margin; compound computer_observe and computer_act default to 30 seconds, and configurable browser/application foreground tools may explicitly request at most 45 seconds. Remote run_process, run_local_command, and exec_command remain durable-first one-step jobs with a 10-second acceptance budget, a separate pre-spawn resource-admission wait of up to 30 minutes, and an independently bounded 1–600-second detached execution timeout. read_job.current_phase=resource_admission means that managed runner is still waiting before child spawn. For ordinary one-step remote process work or any work that must survive relay loss or daemon replacement, bounded same-response read_job follow-up is allowed when terminal state is required; do not busy-loop or infer a host/tool deadline from elapsed wall-clock time, and return active status for later recovery only after an actual host/tool boundary is observed, external input or authorization is required, or the user explicitly requested a checkpoint. list_jobs remains an inventory surface rather than a polling substitute. Use owner-authorized start_job/read_job for multi-step, finally-sensitive work, or a legitimate execution budget above 600 seconds. Machine Bridge cannot observe or guarantee external host receipt of a completed MCP response.",
    "Per-tool success, failure, cancellation, and timing events are debug-only; default logs contain lifecycle and infrastructure events, not a tool-call transcript.",
    "File mutation state failures are typed and privacy-bounded. For write_file, edit_file, and apply_patch, inspect error.code and error.details.reason; do not blindly retry conflict. Re-read the current file or target state, reconcile the intended change, and retry with a fresh precondition.",
    "Inspect before editing, prefer read_file line ranges, edit_file, or apply_patch over whole-file replacement, and report commands that were run."
  ],
  "remoteHttpInitializationCompatibilityVersions": [
    "2025-11-25",
    "2025-06-18"
  ]
}
